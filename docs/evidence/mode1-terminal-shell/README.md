# Mode 1 Terminal Shell Evidence

This folder stores non-sensitive validation evidence for the first Mode 1 terminal shell slice.

Expected evidence:

- command summary from automated validation;
- manual smoke notes;
- screenshots of the branded shell, valid lookup, expired tariff block, mock recalculation, and unsupported profile refusal.
